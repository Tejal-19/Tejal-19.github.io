// @ts-check
import { defineConfig } from 'astro/config';

// `site` is what Astro uses to generate absolute URLs (sitemap, canonical
// links, RSS if we add it later) — it doesn't affect where the files get
// deployed, just what URLs they claim to live at.
export default defineConfig({
  site: 'https://Tejal-19.github.io',
});
